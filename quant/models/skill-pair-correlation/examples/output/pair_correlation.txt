双标的相关性 / 对冲（0700.HK ↔ 9988.HK · 港股）
========================================================
窗口：20260101 ~ 20260526    对齐观测：96 根共同交易日

收益相关性：
  全窗口         0.712
  近 20 日       0.664
  近 60 日       0.731

对冲 beta（A 对 B）：0.884      → 每 1 手 A 约用 0.884 手 B 对冲
最新收盘：A = 402.0    B = 78.5    价格比 = 5.121

对数价差 z-score：-1.83（窗口 60）
  spread = ln(Pa) − beta·ln(Pb)

一句话解读：
  两者中高度相关（0.71），近 20 日相关略降。价差 z = −1.83 < 0，
  A 相对 B 偏“便宜”，接近但未到 ±2 的均值回复触发区（z<−2 才是 long A / short B 的提示）。

说明：
  - 两条腿先按共同交易日 inner-join 对齐，再算统计量，避免日历错配导致的收益错位。
  - 相关性 / beta 为历史值，样本外不稳定；退化腿（方差为 0）对应字段返回 null。
  - 跨市场配对因交易日历差异会缩小对齐样本。
  - 本结果不构成买卖 / 配对 / 对冲指令，仅供研究。
  - 示例数值取自 SKILL.md 输出 schema，非实时行情。
