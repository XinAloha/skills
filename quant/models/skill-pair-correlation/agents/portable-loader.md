# Portable loader

Load `../SKILL.md` as the primary instruction file. Run `../scripts/pair_correlation.py`
with the two user-provided tickers, then verify the reported correlation, hedge beta, and
spread z-score against the aligned sample before presenting the result. Load only the
referenced files needed for the current case. Reproduce the disclaimer from `../README.md`
verbatim; never promise returns.
