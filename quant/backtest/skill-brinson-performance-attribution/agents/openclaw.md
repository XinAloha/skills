# OpenClaw runtime entrypoint

Read `SKILL.md`, then invoke only the documented local CLI or API. Do not access private data or credentials, and do not turn diagnostics into investment recommendations or performance promises.
