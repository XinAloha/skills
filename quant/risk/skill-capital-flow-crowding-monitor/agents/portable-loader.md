# Portable runtime loader

Load `SKILL.md` as the entrypoint, then use `references/` and `scripts/` as needed. This
adapter is compatible with Hermes and OpenClaw and keeps the skill offline by default.
Never invent missing capital-flow observations; report degradation explicitly.
