# Claude Code 入口

本项目的核心技能说明是 `../SKILL.md`。当用户请求单因子回测、股票池审计、IC/IR 分析、因子中性化或未来函数排查时，先读取该文件，再按需读取 `../references/playbook.md` 和 `../references/test-cases.md`。

必须遵守核心说明中的参数、时点、成本和样本外验收要求；输出研究结构和证据链，不输出未经核验的执行指令。
