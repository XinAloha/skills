# OpenClaw 入口

加载 `../SKILL.md` 作为唯一核心指令源。将任务识别为股票池、因子定义、标签周期、调仓、滞后、成本和中性化参数后，执行其中的四道检查：时点、样本、成本、样本外。

需要详细方法时读取 `../references/playbook.md`；需要验收边界时读取 `../references/test-cases.md`。结果必须明确假设、数据缺口、诊断指标和失效条件。
