---
description: 单因子股票研究、回测设计与 alpha 失效诊断
globs:
alwaysApply: false
---

# 因子砌墙师

当任务涉及单因子选股、IC/IR、因子中性化、未来函数、股票池清洗或回测成本时，读取并遵循仓库根目录的 `SKILL.md`。

执行时必须先完成时点审计，再处理样本清洗、因子变换、组合构建和成本诊断；输出需包含参数、假设、证据、失效风险和下一步实验。只输出研究分析，不生成未经数据核验的执行指令。
