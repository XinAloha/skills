# 恢复协议

1. 暂停后读取最后一致状态、租约和证据索引；确认原执行器是否已停止。
2. 对账工作树、分支、外部调用记录和检查点，识别已发生但未持久化的副作用。
3. 先运行只读检查；若状态与现实冲突，记录冲突并转人工，而不是覆盖现实。
4. 依据幂等键选择恢复、补偿、重新验证或升级。不要在未知副作用后直接重试写操作。
5. 收尾时释放租约、保留审计信息，并为下次运行写入明确的 `next_action`。

