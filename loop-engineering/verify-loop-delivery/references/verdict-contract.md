# 裁决契约

每份裁决必须包含：

- `task_id`、实施者身份、验证者身份、基线与变更标识
- `verdict`: `pass`、`reject` 或 `escalate`
- 对每项必需检查的 `rule_id`、命令/查询、结果、证据位置
- 阻断项、未覆盖风险、建议的下一动作
- 验证时间和验证环境

`pass`：所有必需项有通过证据，且无阻断项。

`reject`：至少一项必需检查失败；逐项说明失败证据和实施者可执行的修复方向。

`escalate`：验证身份不独立、能力/证据不足、契约冲突或风险需要人工判断。不得以“看起来不错”替代升级。

